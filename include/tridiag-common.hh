#ifndef _TRIDIAG_COMMON_HH_
#define _TRIDIAG_COMMON_HH_

#define NUM_OF_ARRAY_IN_TRIDIAGS 3

enum index_name { 
  i_ld, // an index for lower offdiagonal array
  i_ud, // an index for upper offdiagonal array
  i_d, // an index for diagonal array
};

#endif // _TRIDIAG_COMMON_HH_
